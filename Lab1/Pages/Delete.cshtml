@page
@model Lab1.Pages.DeleteModel
@{
    ViewData["Title"] = "Подтверждение на удаления языка программирования";
}

<div class="text-center">
    <style>
        body {
            font-family: Roboto, sans-serif;
            background-color: #f4f4f4;
        }
        .my-button {
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .my-button:hover {
            background-color: #0056b3;
        }
        .form-group a {
            margin-left: 15px;
            color: #dc3545;
            text-decoration: none;
        }
        .form-group a:hover {
            text-decoration: underline;
        }
        h1 {
            color: #333;
        }
        p {
            color: #666;
        }
        dl {
            color: #333;
        }
        dt {
            color: black;
            font-size: 18px;
            margin-top: 10px;
        }
        dd {
            color: darkred;
            font-size: 16px;
            margin-left: 20px;
        }
        h4{
            color: black;
            font-size: 31px;
            margin-top: 10px;
        }
    </style>
</div>

 <h1>@ViewData["Title"]</h1> 

<p>Уверены, что хотите именно эту удалить язык программирования?</p>

<div>
    <h4>@Model.Language.Name</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">тип</dt>
        <dd class="col-sm-10">@Model.Language.Type</dd>
        <dt class="col-sm-2">рейтинг</dt>
        <dd class="col-sm-10">@Model.Language.Rating</dd>
    </dl>

    <form method="post" onsubmit="return confirm('Уверены, что хотите именно эту удалить язык программирования?');">
        <input type="hidden" asp-for="Language.Id" />
        <input type="hidden" name="confirm" value="yes" />
        <input type="submit" value="удалить" class="my-button" /> |
        <a asp-page="./Index">отмена</a>
    </form>
</div>

